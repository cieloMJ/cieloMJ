import sys
N= int(sys.stdin.readline())
ball = list(map(int, sys.stdin.readline().split()))
result = []
for i in range(N):
    pos = i - ball[i]
    result.insert(pos, i+1)

print(*result)

# append vs insert
# 둘의 공통점은 리스트에 원소를 추가하는 것이 가능하다는 것이다.
# 다만 append는 리스트의 맨 뒤에 원소를 추가하며 위치를 지정할 수 없고,
#insert(idx, v)로 원하는 위치에 값을 넣을 수 있다.
## 해당 인덱스에 v 값을 넣는 경우 기존의 원소들은 오른쪽으로 밀리게 된다.

# 삽입 위치를 결정할 수 있는가의 여부에 따라,
# 시간 복잡도는 append O(1), insert O(n)으로 insert는 불필요한 사용을 지양할 필요가 있다.
